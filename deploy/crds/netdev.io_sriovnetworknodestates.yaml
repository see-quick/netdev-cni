---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sriovnetworknodestates.netdev.io
spec:
  group: netdev.io
  names:
    kind: SriovNetworkNodeState
    listKind: SriovNetworkNodeStateList
    plural: sriovnetworknodestates
    singular: sriovnetworknodestate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SriovNetworkNodeState is the per-node VF allocation state, written
          by the node agent.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              interfaces:
                description: Interfaces is written by the operator to instruct the
                  node agent.
                items:
                  properties:
                    deviceType:
                      type: string
                    name:
                      type: string
                    numVfs:
                      type: integer
                  required:
                  - deviceType
                  - name
                  - numVfs
                  type: object
                type: array
            type: object
          status:
            properties:
              interfaces:
                description: Interfaces reflects the actual VF state on this node.
                items:
                  properties:
                    allocatedVfs:
                      type: integer
                    deviceType:
                      type: string
                    name:
                      type: string
                    totalVfs:
                      type: integer
                    vfs:
                      items:
                        properties:
                          allocated:
                            description: Allocated is true when this VF is assigned
                              to a pod.
                            type: boolean
                          id:
                            description: ID is the VF index (0-based).
                            type: integer
                          name:
                            description: Name is the interface name (e.g. "eth1v0"
                              or "sim_vf0").
                            type: string
                          pciAddress:
                            description: PCIAddress is set for vfio-pci device type.
                            type: string
                          podRef:
                            description: PodRef is the namespace/name of the pod that
                              holds this VF.
                            type: string
                        required:
                        - allocated
                        - id
                        - name
                        type: object
                      type: array
                  required:
                  - allocatedVfs
                  - deviceType
                  - name
                  - totalVfs
                  type: object
                type: array
              simulationMode:
                description: SimulationMode is true when the node agent is running
                  in simulation mode.
                type: boolean
              syncStatus:
                description: SyncStatus is "Succeeded", "Failed", or "InProgress".
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
